/**
 * Placeholder Supabase types — will be replaced by `npm run supabase:types`
 * after Phase 2 migrations are applied to gzmgjkbtsvezfclmreru.
 *
 * Until then, keeping a minimal Database type so the supabase client compiles.
 */
export type Database = {
  public: {
    Tables: Record<string, never>
    Views: Record<string, never>
    Functions: Record<string, never>
    Enums: Record<string, never>
  }
}
