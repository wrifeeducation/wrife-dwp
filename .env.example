# ─── Shared WriFe Platform Supabase project ──────────────────────────
# DWP shares the same Supabase instance as PWP Studio, Interactive Practice,
# and wrife.co.uk — project ref: gzmgjkbtsvezfclmreru
# Get these from the Supabase dashboard → Project Settings → API

VITE_SUPABASE_URL=https://gzmgjkbtsvezfclmreru.supabase.co
VITE_SUPABASE_ANON_KEY=<anon/public key from Supabase dashboard>

# Public site URL (used for OAuth redirects and Route A hash-token detection)
VITE_PUBLIC_SITE_URL=https://dailywrite.wrife.co.uk

# wrife.co.uk hub URL — used by the ← WriFe back button when entry is via Route A
VITE_WRIFE_HUB_URL=https://wrife.co.uk

# Server-only — set in Vercel project environment variables, NOT here
ANTHROPIC_API_KEY=sk-ant-...
ELEVENLABS_API_KEY=...
ELEVENLABS_VOICE_ID=...
